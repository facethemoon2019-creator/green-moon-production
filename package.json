{
  "name": "green-moon-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute green-moon-db --remote --file=./migrations/001_init.sql"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}